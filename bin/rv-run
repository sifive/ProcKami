#! /usr/local/bin/ruby

fn = ARGV[0]
(puts "rv-run: no file given"; exit) if fn.nil?
fn = fn[0...-4] if fn[-4..-1]=='.hex'

system("mv #{fn}.hex ./MemoryInit.hex") || (puts "rv-run: #{fn}.hex: mv failed"; exit)
